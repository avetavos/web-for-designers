---
title: 'Generics & Trait Bounds'
description: 'Generic functions and structs with trait bounds (`<T: Display>`, `where`), compared to TypeScript generics and constraints.'
---

import TsGo from '../../../../components/TsGo.tsx';
import Playground from '../../../../components/Playground.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import RsOnly from '../../../../components/RsOnly.astro';

## TypeScript generics vs Rust generics

In TypeScript you write `function identity<T>(x: T): T` to make a function work for any type. Rust uses the same angle-bracket syntax: `fn identity<T>(x: T) -> T`. The key difference is that TypeScript's generics are partially erased at compile time and rely on structural typing, while Rust's are **monomorphised** — the compiler generates a completely separate, concrete function for every type you use. You pay zero overhead at runtime.

## Trait bounds: constraining `T`

TypeScript constrains generics with `extends`: `<T extends Printable>`. Rust constrains with **trait bounds**: `<T: Display>` (inline syntax) or a `where` clause (separate line, preferred for readability).

export const genericTs = `// TypeScript — constraint with extends
interface Printable {
  toString(): string;
}

function printTwice<T extends Printable>(value: T): void {
  console.log(value.toString());
  console.log(value.toString());
}

printTwice(42);
printTwice("hello");`;

export const genericRs = `use std::fmt::Display;

// Inline bound syntax
fn print_twice<T: Display>(value: T) {
    println!("{value}");
    println!("{value}");
}

// where clause — cleaner for multiple bounds
fn print_and_debug<T>(value: T)
where
    T: Display + std::fmt::Debug,
{
    println!("display: {value}");
    println!("debug:   {value:?}");
}

fn main() {
    print_twice(42);
    print_twice("hello");
    print_and_debug(3.14_f64);
}`;

<TsGo client:visible ts={genericTs} go={genericRs} />

## Generic structs

Just like TypeScript classes can be generic (`class Stack<T>`), Rust structs can be generic too. You add the type parameter after the struct name and repeat it in the `impl` block.

export const structTs = `// TypeScript generic class
class Stack<T> {
  private items: T[] = [];
  push(item: T): void { this.items.push(item); }
  pop(): T | undefined { return this.items.pop(); }
  peek(): T | undefined { return this.items[this.items.length - 1]; }
}

const s = new Stack<number>();
s.push(1);
s.push(2);
console.log(s.pop()); // 2`;

export const structRs = `struct Stack<T> {
    items: Vec<T>,
}

impl<T> Stack<T> {
    fn new() -> Self {
        Stack { items: Vec::new() }
    }
    fn push(&mut self, item: T) {
        self.items.push(item);
    }
    fn pop(&mut self) -> Option<T> {
        self.items.pop()
    }
    fn peek(&self) -> Option<&T> {
        self.items.last()
    }
}

fn main() {
    let mut s: Stack<i32> = Stack::new();
    s.push(1);
    s.push(2);
    println!("{:?}", s.pop()); // Some(2)
}`;

<TsGo client:visible ts={structTs} go={structRs} />

## Multiple trait bounds and the `+` syntax

You can require a type to implement several traits at once by joining them with `+`, just like TypeScript's `T extends A & B`.

```
// TypeScript
function foo<T extends Serializable & Loggable>(x: T) { ... }

// Rust — inline
fn foo<T: Serialize + Debug>(x: T) { ... }

// Rust — where clause (preferred when bounds get long)
fn foo<T>(x: T)
where
    T: Serialize + Debug + Clone,
{ ... }
```

<RsOnly title="Monomorphisation: zero-cost generics">

When you write `print_twice::<i32>(42)` and `print_twice::<&str>("hi")`, the compiler generates two separate machine-code functions — one for `i32`, one for `&str`. There is no dynamic dispatch, no virtual table lookup, no boxing. This is called **monomorphisation**, and it is why Rust generics are described as "zero-cost abstractions."

The trade-off: compile times are longer for codebases with many distinct generic instantiations, and binary size grows proportionally to the number of unique type combinations.

</RsOnly>

## Try it

export const genericPlayground = `use std::fmt::Display;

fn largest<T: PartialOrd>(list: &[T]) -> &T {
    let mut largest = &list[0];
    for item in list {
        if item > largest {
            largest = item;
        }
    }
    largest
}

fn describe<T: Display + std::fmt::Debug>(value: T) {
    println!("Display: {value}");
    println!("Debug:   {value:?}");
}

fn main() {
    let numbers = vec![34, 50, 25, 100, 65];
    println!("largest number: {}", largest(&numbers));

    let chars = vec!['y', 'm', 'a', 'q'];
    println!("largest char: {}", largest(&chars));

    describe(42_i32);
    describe("hello");
}`;

<Playground client:visible code={genericPlayground} />

export const quizGenerics = [
  {
    q: "What does Rust's monomorphisation mean for generic functions?",
    options: [
      "One function handles all types via dynamic dispatch at runtime",
      "The compiler generates a separate concrete function per type used",
      "Generic code is slower than non-generic code",
      "Type parameters are erased like in Java"
    ],
    answer: 1
  },
  {
    q: "Which of these correctly adds two trait bounds to a generic parameter?",
    options: [
      "`<T: Display, Debug>`",
      "`<T extends Display + Debug>`",
      "`<T: Display + Debug>`",
      "`<T implements Display & Debug>`"
    ],
    answer: 2
  },
  {
    q: "When should you prefer a `where` clause over inline bounds?",
    options: [
      "Always — inline bounds are deprecated",
      "When a function has multiple type parameters or long bounds, for readability",
      "Only for structs, never for functions",
      "When you have exactly one bound"
    ],
    answer: 1
  },
  {
    q: "What is the TypeScript equivalent of Rust's `<T: Serialize + Debug>`?",
    options: [
      "`<T extends Serialize | Debug>`",
      "`<T extends Serialize & Debug>`",
      "`<T implements Serialize, Debug>`",
      "`<T: Serialize, T: Debug>`"
    ],
    answer: 1
  }
];

<Quiz client:visible id="advanced/generics-trait-bounds" questions={quizGenerics} />

<ProgressTracker client:visible id="advanced/generics-trait-bounds" />
