---
title: 'Accessibility'
description: 'Practical accessibility for designers — alt text, color contrast, focus states, labels, and semantic structure.'
---

import LivePreview from '../../../../components/LivePreview.tsx';
import Tip from '../../../../components/Tip.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

Accessibility (a11y) means designing so that everyone can use your product — people who navigate by keyboard instead of mouse, people who use screen readers, people with low vision or color blindness, people on slow connections. It is not a niche concern: roughly one in four adults has some form of disability, and accessible design is simply good design.

The good news: most of the practical wins are not hard. They just require the habit of thinking about them.

## Alt text for images

Screen readers announce images by reading the `alt` attribute. Without it, a screen reader user either hears nothing or hears a long cryptic filename.

```html
<!-- Bad -->
<img src="hero.jpg" />

<!-- Good -->
<img src="hero.jpg" alt="A designer sketching wireframes on paper" />
```

**Rule:** describe what the image shows and why it matters in context. For decorative images (background textures, purely visual dividers), use `alt=""` — that tells the screen reader to skip it.

## Color contrast

WCAG (Web Content Accessibility Guidelines) defines minimum contrast ratios:
- **4.5:1** — normal body text (below 18px or 14px bold)
- **3:1** — large text (18px+ regular, 14px+ bold), UI components, icons

In Figma, use the Contrast plugin or check contrast in Design > Inspect. A purple `#7C3AED` button with white text passes at large size but check small text carefully.

## Labels for form inputs

Every form input needs a visible `<label>` linked to its input. Placeholder text is not a label — it disappears when the user types and has low contrast.

```html
<!-- Bad: no label -->
<input type="email" placeholder="Email address" />

<!-- Good: proper label -->
<label for="email">Email address</label>
<input type="email" id="email" placeholder="you@example.com" />
```

## Focus states

Users who navigate by keyboard need to see which element is currently focused. Browsers provide a default outline, but many designers remove it with `outline: none` — breaking keyboard navigation.

Keep focus visible. Style it to match your brand instead of removing it:

```css
.btn:focus-visible {
  outline: 3px solid #7C3AED;
  outline-offset: 2px;
}
```

## Semantic structure

Use heading elements (`<h1>` through `<h6>`) for hierarchy — not for their size. Screen readers use headings to let users jump around the page, like a table of contents. A page should have exactly one `<h1>`, and headings should nest logically.

Also use landmark elements: `<main>`, `<nav>`, `<header>`, `<footer>`, `<aside>`. They let screen reader users skip directly to the content they need.

## Font size

Never set body text below 16px. On mobile, this is critical — small text forces users to pinch-zoom, which breaks layout and creates a frustrating experience.

## A form with good accessibility

The form below demonstrates labeled inputs, proper contrast, and a styled focus ring. Tab through the fields to see the focus indicator.

export const a11yHtml = `<form class="contact-form">
  <div class="field">
    <label class="label" for="name">Full name</label>
    <input class="input" type="text" id="name" name="name" placeholder="Alex Chen" />
  </div>
  <div class="field">
    <label class="label" for="email">Email address</label>
    <input class="input" type="email" id="email" name="email" placeholder="you@example.com" />
  </div>
  <div class="field">
    <label class="label" for="message">Message</label>
    <textarea class="input" id="message" name="message" rows="3" placeholder="Tell me about your project..."></textarea>
  </div>
  <button class="btn" type="submit">Send message</button>
</form>`;

export const a11yCss = `body { font-family: system-ui, sans-serif; padding: 2rem; background: #f5f5f5; }
.contact-form {
  background: #fff;
  padding: 1.5rem;
  border-radius: 12px;
  max-width: 400px;
  display: flex;
  flex-direction: column;
  gap: 1rem;
}
.field { display: flex; flex-direction: column; gap: 0.375rem; }
.label { font-size: 0.875rem; font-weight: 600; color: #111827; }
.input {
  font-size: 1rem;
  font-family: inherit;
  padding: 0.5rem 0.75rem;
  border: 1.5px solid #D1D5DB;
  border-radius: 6px;
  color: #111827;
  background: #fff;
}
.input:focus {
  outline: 3px solid #7C3AED;
  outline-offset: 1px;
  border-color: #7C3AED;
}
.btn {
  background: #7C3AED;
  color: #ffffff;
  font-size: 1rem;
  font-weight: 600;
  font-family: inherit;
  padding: 0.625rem 1.25rem;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  align-self: flex-start;
}
.btn:focus-visible {
  outline: 3px solid #7C3AED;
  outline-offset: 3px;
}
.btn:hover { background: #6D28D9; }`;

<LivePreview client:visible html={a11yHtml} css={a11yCss} />

Tab through the form using only your keyboard. Each field should show a clear purple outline when focused. This is what keyboard-only users rely on.

<Tip title="Accessibility is a quality dimension, not an afterthought">
Accessible design is not a feature you bolt on at the end of a project. It is a quality dimension — like visual polish or performance. The best time to add it is in Figma, before a single line of code is written. Define focus states, check contrast, name your components clearly. Your developer will thank you, and so will your users.
</Tip>

export const quizAccessibility = [
  {
    q: 'What does the `alt` attribute on an `<img>` do?',
    options: [
      'Changes the image size',
      'Provides a text description for screen readers and when images fail to load',
      'Adds a caption below the image',
      'Links the image to another page',
    ],
    answer: 1,
  },
  {
    q: 'WCAG recommends a minimum contrast ratio of ____ for normal body text.',
    options: ['2:1', '3:1', '4.5:1', '7:1'],
    answer: 2,
  },
  {
    q: 'Why is semantic HTML (like <h1>, <nav>, <main>) important for accessibility?',
    options: [
      'It makes the page look better',
      'It gives screen readers meaningful context about content structure',
      'It reduces file size',
      'It is required by browsers',
    ],
    answer: 1,
  },
  {
    q: 'Which CSS property makes a visible focus indicator?',
    options: ['focus-color', 'outline', 'border-focus', 'ring'],
    answer: 1,
  },
];

<Quiz client:visible id="workflow/accessibility" questions={quizAccessibility} />

<ProgressTracker client:visible id="workflow/accessibility" />
