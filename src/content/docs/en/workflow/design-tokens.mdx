---
title: 'Design Tokens'
description: 'CSS custom properties are the code version of Figma variables and styles — define once, reuse everywhere.'
---

import LivePreview from '../../../../components/LivePreview.tsx';
import Tip from '../../../../components/Tip.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

In Figma, when you define a color style called `brand/primary` and apply it to thirty elements, you can change that color once and all thirty elements update. That is the power of a single source of truth. CSS custom properties — also called **design tokens** or **CSS variables** — give you the exact same superpower in code.

## Figma variables → CSS custom properties

| Figma concept | CSS equivalent |
|---|---|
| Color Style `brand/primary` | `--color-primary: #7C3AED;` |
| Color Style `text/muted` | `--color-text-muted: #6B7280;` |
| Spacing variable `space/4` | `--space-4: 1rem;` |
| Radius variable `radius/card` | `--radius-card: 12px;` |

You define all tokens once at the top of your CSS, on the special `:root` selector. `:root` means "the entire document" — so these values are available everywhere.

```css
:root {
  --color-primary: #7C3AED;
  --color-text: #111827;
  --color-text-muted: #6B7280;
  --color-surface: #FFFFFF;
  --space-2: 0.5rem;
  --space-4: 1rem;
  --space-6: 1.5rem;
  --radius-card: 12px;
}
```

To use a token, write `var(--token-name)` wherever you would normally write the value:

```css
.card {
  background: var(--color-surface);
  padding: var(--space-6);
  border-radius: var(--radius-card);
}
```

## Why this matters

When your brand color changes from purple to teal, you change **one line** — `--color-primary` — and every button, link, and highlight on every page updates automatically. Without tokens, you would need to find and replace every hardcoded `#7C3AED` in every CSS file.

This is exactly why design systems exist: define decisions once, enforce them everywhere.

## Tokens in action

The card below is built entirely from tokens. Try changing `--color-primary` in the CSS panel — notice how the button and the tag both update at once.

export const tokensHtml = `<div class="card">
  <span class="tag">Product Design</span>
  <h3 class="card-title">Design Systems</h3>
  <p class="card-body">Building a single source of truth for color, spacing, and type.</p>
  <a class="btn-primary" href="#">Read more</a>
</div>`;

export const tokensCss = `:root {
  --color-primary: #7C3AED;
  --color-primary-text: #ffffff;
  --color-surface: #ffffff;
  --color-text: #111827;
  --color-text-muted: #6B7280;
  --color-tag-bg: #EDE9FE;
  --color-tag-text: #5B21B6;
  --space-2: 0.5rem;
  --space-3: 0.75rem;
  --space-4: 1rem;
  --space-6: 1.5rem;
  --radius-sm: 4px;
  --radius-card: 12px;
}
body { font-family: system-ui, sans-serif; padding: var(--space-6); background: #f5f5f5; }
.card {
  background: var(--color-surface);
  padding: var(--space-6);
  border-radius: var(--radius-card);
  box-shadow: 0 2px 12px rgba(0,0,0,0.08);
  max-width: 320px;
  display: flex;
  flex-direction: column;
  gap: var(--space-3);
}
.tag {
  display: inline-block;
  background: var(--color-tag-bg);
  color: var(--color-tag-text);
  font-size: 0.75rem;
  font-weight: 600;
  padding: 0.25rem var(--space-2);
  border-radius: var(--radius-sm);
}
.card-title { margin: 0; font-size: 1.125rem; color: var(--color-text); }
.card-body { margin: 0; font-size: 0.875rem; color: var(--color-text-muted); line-height: 1.6; }
.btn-primary {
  display: inline-block;
  background: var(--color-primary);
  color: var(--color-primary-text);
  padding: var(--space-2) var(--space-4);
  border-radius: var(--radius-sm);
  text-decoration: none;
  font-size: 0.875rem;
  font-weight: 600;
}`;

<LivePreview client:visible html={tokensHtml} css={tokensCss} />

<Tip title="If you use a color more than once, it should be a token">
Hardcoded values like `#7C3AED` scattered across your CSS are design debt. Every time the brand color changes, you will miss one. Tokens are not overhead — they are how you maintain a design system in code without breaking things.
</Tip>

export const quizDesignTokens = [
  {
    q: 'Where do you typically define CSS custom properties (tokens)?',
    options: [
      'Inside a button rule',
      'On the :root selector',
      'In the HTML head',
      'Inside a media query',
    ],
    answer: 1,
  },
  {
    q: 'How do you use a CSS custom property named --color-brand?',
    options: [
      'color: --color-brand',
      'color: var(--color-brand)',
      'color: token(--color-brand)',
      'color: $color-brand',
    ],
    answer: 1,
  },
  {
    q: 'What is the main benefit of using design tokens?',
    options: [
      'They make the page load faster',
      'They let you change a value in one place and update everywhere',
      'They add animations automatically',
      'They replace HTML',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="workflow/design-tokens" questions={quizDesignTokens} />

<ProgressTracker client:visible id="workflow/design-tokens" />
