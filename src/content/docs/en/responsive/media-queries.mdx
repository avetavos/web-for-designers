---
title: Media Queries
description: 'Use @media rules to apply different CSS at different screen widths — the direct code equivalent of your Figma artboards.'
---

import LivePreview from '../../../../components/LivePreview.tsx';
import Tip from '../../../../components/Tip.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

In Figma you have separate artboards for desktop, tablet, and mobile. You decide which layers are visible, which fonts are larger, which columns collapse. A **media query** is how you express exactly that logic in CSS — "apply *these* rules when the screen is *this* wide".

## The syntax

```css
@media (max-width: 480px) {
  /* rules inside here only apply when the viewport is 480 px wide or narrower */
}

@media (min-width: 480px) {
  /* rules inside here only apply when the viewport is 480 px wide or wider */
}
```

Think of it as an `if` statement for CSS:

- `max-width` = "if the screen is **at most** this wide, do this" (large → small)
- `min-width` = "if the screen is **at least** this wide, do this" (small → large)

## A real example — card that changes layout

The preview pane below is your screen. **Make your browser window narrower** (or view on a phone) and watch the card reflow. Because the preview pane is itself about half the page width on a desktop monitor, the `max-width: 420px` breakpoint will trigger there — you can see the effect without needing a phone.

export const mediaQueriesHtml = `<div class="card">
  <div class="card__image"></div>
  <div class="card__body">
    <h2 class="card__title">Sunset over the hills</h2>
    <p class="card__text">A short description of the photo that wraps naturally on small screens.</p>
    <a class="card__btn" href="#">View full size</a>
  </div>
</div>`;

export const mediaQueriesCss = `body {
  font-family: system-ui, sans-serif;
  margin: 0;
  padding: 1rem;
  background: #f1f5f9;
}

/* Default (wider) layout: image left, text right */
.card {
  display: flex;
  flex-direction: row;
  gap: 1rem;
  background: #fff;
  border-radius: 12px;
  overflow: hidden;
  box-shadow: 0 2px 8px rgba(0,0,0,.08);
  padding: 1rem;
}

.card__image {
  flex: 0 0 140px;
  height: 140px;
  background: linear-gradient(135deg, #7C3AED, #2563EB);
  border-radius: 8px;
}

.card__body {
  display: flex;
  flex-direction: column;
  gap: 0.5rem;
}

.card__title {
  margin: 0;
  font-size: 1.1rem;
  color: #1e293b;
}

.card__text {
  margin: 0;
  font-size: 0.875rem;
  color: #64748b;
}

.card__btn {
  display: inline-block;
  margin-top: auto;
  padding: 0.4rem 0.9rem;
  background: #7C3AED;
  color: #fff;
  border-radius: 6px;
  text-decoration: none;
  font-size: 0.8rem;
  font-weight: 600;
}

/* Narrow layout: stack vertically */
@media (max-width: 420px) {
  .card {
    flex-direction: column;
  }

  .card__image {
    flex: 0 0 auto;
    width: 100%;
    height: 120px;
    border-radius: 8px;
  }
}`;

<LivePreview client:visible html={mediaQueriesHtml} css={mediaQueriesCss} height="280px" />

At wider widths the image and text sit side by side. When the pane narrows past 420 px, the single `@media (max-width: 420px)` block kicks in and stacks them vertically. This is exactly what you would show a developer by switching between your mobile and desktop artboards in Figma.

## Changing typography too

You are not limited to layout. You can change font sizes, hide elements, adjust spacing — anything in CSS can be overridden inside a media query.

```css
h1 {
  font-size: 2.5rem;   /* default: large screen */
}

@media (max-width: 420px) {
  h1 {
    font-size: 1.6rem;  /* smaller on narrow screens */
  }
}
```

<Tip title="Use 420 px breakpoints in the preview, not 768 px">
The live preview pane in this course is roughly half the page width on a desktop — usually 400–500 px wide. A breakpoint of 768 px will never trigger inside the preview because the pane is already narrower than that. Use breakpoints around 400–480 px in these examples so you can observe the change without needing a phone. In real projects you will use standard breakpoints (640 px, 768 px, 1024 px), but for learning, keep them small.
</Tip>

export const quizMediaQueries = [
  {
    q: 'What does `@media (max-width: 480px)` mean?',
    options: [
      'Apply these rules only on screens wider than 480 px',
      'Apply these rules only on screens 480 px wide or narrower',
      'Set the maximum width of the page to 480 px',
      'Hide everything wider than 480 px',
    ],
    answer: 1,
  },
  {
    q: 'In the card example, what CSS change triggers on narrow screens?',
    options: [
      'The card background colour changes',
      'The card text disappears',
      'The flex-direction changes from row to column',
      'The image is hidden',
    ],
    answer: 2,
  },
  {
    q: 'Why are small breakpoints (around 420 px) recommended for the live preview in this course?',
    options: [
      'Because 420 px is the standard mobile breakpoint',
      'Because the preview pane is already narrow, so 768 px breakpoints would never trigger',
      'Because smaller breakpoints load faster',
      'Because 420 px matches the iPhone screen size exactly',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="responsive/media-queries" questions={quizMediaQueries} />

<ProgressTracker client:visible id="responsive/media-queries" />
