---
title: 'Control Flow'
description: 'if as an expression, match, loop, while, and for in Rust vs TypeScript.'
---

import TsGo from '../../../../components/TsGo.tsx';
import Playground from '../../../../components/Playground.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## Control flow in TypeScript vs Rust

Rust has the same control-flow constructs you know — `if`, `while`, `for` — but with two key differences: `if` and `match` are **expressions** that produce values, and `match` is **exhaustive** (the compiler errors if you miss a case).

## if as an expression

In TypeScript you reach for the ternary `? :` when you need a conditional value. In Rust, `if` itself is an expression — every branch must produce the same type.

```
// TypeScript
const label = score >= 60 ? "pass" : "fail";

// Rust
let label = if score >= 60 { "pass" } else { "fail" };
```

## match — exhaustive switch

`match` in Rust is like TypeScript's `switch`, but exhaustive and expression-oriented. The compiler guarantees every possible value is handled. Patterns can be ranges, literals, enums, and more.

## loop, while, for

- `loop` runs forever until a `break`. Unlike a `while true`, `loop` can return a value via `break value`.
- `while` works as expected.
- `for item in collection` iterates over any iterable — no index arithmetic needed.

export const controlFlowTs = `const score = 85;
const grade = score >= 90 ? "A" : score >= 80 ? "B" : "C";
console.log(grade); // "B"

// switch (not exhaustive by default)
switch (score) {
  case 100: console.log("perfect"); break;
  default:  console.log("ok");
}`;

export const controlFlowRs = `fn main() {
    let score = 85;
    // if is an expression
    let grade = if score >= 90 { "A" } else if score >= 80 { "B" } else { "C" };
    println!("{grade}"); // B

    // loop with a break value
    let mut counter = 0;
    let result = loop {
        counter += 1;
        if counter == 5 { break counter * 2; }
    };
    println!("{result}"); // 10

    // for over a range
    for i in 1..=3 {
        println!("{i}");
    }

    // match is exhaustive
    match score {
        90..=100 => println!("A"),
        80..=89  => println!("B"),
        _        => println!("C or below"),
    }
}`;

<TsGo client:visible ts={controlFlowTs} go={controlFlowRs} />

## Try it

export const controlFlowCode = `fn classify(n: i32) -> &'static str {
    match n {
        i32::MIN..=-1 => "negative",
        0 => "zero",
        1..=9 => "small positive",
        _ => "large positive",
    }
}

fn main() {
    let score = 85;
    let grade = if score >= 90 { "A" } else if score >= 80 { "B" } else { "C" };
    println!("Grade: {grade}");

    for n in [0, -3, 5, 42] {
        println!("{n} is {}", classify(n));
    }
}`;

<Playground client:visible code={controlFlowCode} />

export const quizControlFlow = [
  {
    q: "What happens if you forget a case in a Rust `match` expression?",
    options: [
      "The program panics at runtime",
      "The missing case returns unit ()",
      "The compiler produces an error",
      "The match falls through to the next arm"
    ],
    answer: 2
  },
  {
    q: "How do you return a value from a `loop` in Rust?",
    options: [
      "return value; inside the loop",
      "break value; inside the loop",
      "You cannot return a value from loop",
      "yield value;"
    ],
    answer: 1
  },
  {
    q: "Which of the following is valid Rust?",
    options: [
      "let x = if true { 1 };",
      "let x = if true { 1 } else { 2 };",
      "let x = if true { 1 } else { \"two\" };",
      "let x = if true 1 else 2;"
    ],
    answer: 1
  }
];

<Quiz client:visible id="rust-101/control-flow" questions={quizControlFlow} />

<ProgressTracker client:visible id="rust-101/control-flow" />
