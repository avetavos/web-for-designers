---
title: 'Enums and Match'
description: 'Enums with data variants, exhaustive match, and if let in Rust vs TypeScript discriminated unions.'
---

import TsGo from '../../../../components/TsGo.tsx';
import Playground from '../../../../components/Playground.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import RsOnly from '../../../../components/RsOnly.astro';

## Enums in TypeScript vs Rust

TypeScript has discriminated unions: a type union where each member has a literal `kind` field that narrows the type. Rust enums are first-class: each variant is a named type that can carry data directly, with no manual `kind` field needed.

## Enums with data

In TypeScript you write a union of object types. In Rust, each enum variant can hold tuple-style data, named fields, or nothing at all:

```
// TypeScript discriminated union
type Shape =
  | { kind: "circle"; radius: number }
  | { kind: "rectangle"; width: number; height: number };

// Rust enum with data variants
enum Shape {
    Circle(f64),
    Rectangle(f64, f64),
}
```

## Exhaustive match

When you `match` on a Rust enum, the compiler requires you to handle every variant. Forget one and the build fails.

## if let

`if let` is shorthand for a `match` with one arm you care about and a fallback you don't:

```
if let Shape::Circle(r) = shape {
    println!("radius is {r}");
}
```

export const enumsTs = `type Shape =
  | { kind: "circle"; radius: number }
  | { kind: "rectangle"; width: number; height: number };

function area(shape: Shape): number {
  switch (shape.kind) {
    case "circle":    return Math.PI * shape.radius ** 2;
    case "rectangle": return shape.width * shape.height;
    // TypeScript does not enforce exhaustiveness by default
  }
}`;

export const enumsRs = `enum Shape {
    Circle(f64),
    Rectangle(f64, f64),
}

fn area(shape: &Shape) -> f64 {
    match shape {
        Shape::Circle(r)       => std::f64::consts::PI * r * r,
        Shape::Rectangle(w, h) => w * h,
        // forgetting a variant is a compile error
    }
}`;

<TsGo client:visible ts={enumsTs} go={enumsRs} />

## Try it

export const enumsCode = `enum Shape {
    Circle(f64),
    Rectangle(f64, f64),
}

fn area(shape: &Shape) -> f64 {
    match shape {
        Shape::Circle(r) => std::f64::consts::PI * r * r,
        Shape::Rectangle(w, h) => w * h,
    }
}

fn main() {
    let c = Shape::Circle(5.0);
    let r = Shape::Rectangle(4.0, 6.0);
    println!("Circle area: {:.2}", area(&c));
    println!("Rectangle area: {:.2}", area(&r));
}`;

<Playground client:visible code={enumsCode} />

<RsOnly title="Algebraic Data Types and exhaustive matching">

TypeScript discriminated unions are a convention — you add a `kind` field and TypeScript narrows the type for you. The switch/if-else is still a plain runtime construct, and exhaustiveness checking requires `never` tricks or `switch-exhaustive-cases` lint rules.

Rust enums are **algebraic data types (ADTs)** — a formal feature of the type system. Each variant is a distinct type. `match` is part of the language semantics, not a library, and exhaustiveness is **enforced by the compiler** with no extra setup. You cannot forget a case and ship.

Variants can hold different shapes of data with no boilerplate:

```
enum Message {
    Quit,                        // unit variant
    Move { x: i32, y: i32 },    // named fields
    Write(String),               // tuple variant
    ChangeColor(u8, u8, u8),     // multi-value tuple
}
```

</RsOnly>

export const quizEnums = [
  {
    q: "What happens if you miss a variant in a Rust `match` on an enum?",
    options: [
      "The missing variant is silently ignored",
      "The program panics at runtime",
      "The compiler produces a non-exhaustive match error",
      "The default arm handles it automatically"
    ],
    answer: 2
  },
  {
    q: "What is `if let` in Rust?",
    options: [
      "A way to conditionally declare a mutable variable",
      "Shorthand for a match with one arm you care about",
      "A loop that runs while a pattern matches",
      "Syntax for optional chaining"
    ],
    answer: 1
  },
  {
    q: "Which Rust enum variant syntax carries named fields?",
    options: [
      "Variant(T)",
      "Variant { field: T }",
      "Variant: T",
      "Variant<T>"
    ],
    answer: 1
  }
];

<Quiz client:visible id="rust-101/enums-match" questions={quizEnums} />

<ProgressTracker client:visible id="rust-101/enums-match" />
