---
title: 'Type & Spacing Scales'
description: 'ระบบ 4/8px และ type scale ที่แสดงออกมาเป็น tokens'
---

import LivePreview from '../../../../components/LivePreview.tsx';
import Tip from '../../../../components/Tip.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

เมื่อคุณ nudge element ใน Figma ค่า nudge เริ่มต้นคือ 8px นั่นไม่ใช่เรื่องบังเอิญ ตาราง 8px คือระบบ spacing ที่พบมากที่สุดในการออกแบบมืออาชีพ — และมันมีคู่เทียบในโค้ดโดยตรงที่ทำให้เลย์เอาต์ของคุณสอดคล้องกัน

## Spacing scale

Spacing scale คือชุดค่าคงที่ที่สร้างบน base unit หน่วยฐานที่พบมากที่สุดคือ **4px** (โดย 8px เป็น "standard step") นี่คือวิธีที่มันแมป:

| ชื่อ Token | ค่า | Figma equivalent |
|---|---|---|
| `--space-1` | `4px` | Nudge × 0.5 |
| `--space-2` | `8px` | 1 nudge |
| `--space-3` | `12px` | 1.5 nudges |
| `--space-4` | `16px` | 2 nudges |
| `--space-6` | `24px` | 3 nudges |
| `--space-8` | `32px` | 4 nudges |
| `--space-12` | `48px` | 6 nudges |
| `--space-16` | `64px` | 8 nudges |

ทุกอย่างในเลย์เอาต์ของคุณ — padding, margin, gap ระหว่าง elements — ควรใช้หนึ่งในค่าเหล่านี้ ค่าที่ "นอก scale" (เช่น 13px หรือ 22px) มักเป็นข้อผิดพลาดในการออกแบบที่คุ้มค่าแก่การแก้ไข

## Type scale

Type scale คือชุดขนาดฟอนต์ที่สอดคล้องกัน สร้างบน ratio ที่กำหนด Ratio ที่พบบ่อยคือ **1.25** (Major Third) และ **1.5** (Perfect Fifth) โดยเริ่มจาก base 16px:

| ชื่อ Token | ค่า (ratio 1.25) | การใช้งาน |
|---|---|---|
| `--text-xs` | `12px` | Labels, captions |
| `--text-sm` | `14px` | Body รอง, metadata |
| `--text-base` | `16px` | Body text |
| `--text-lg` | `20px` | Lead text, card headings |
| `--text-xl` | `24px` | Section headings |
| `--text-2xl` | `32px` | Page headings |
| `--text-3xl` | `40px` | Hero headlines |

ใน Figma สิ่งเหล่านี้แมปกับ **Text Styles** ของคุณ ถ้า text styles ของคุณมีชื่อว่า `body/sm`, `body/base`, `heading/lg` ควรแปลโดยตรงเป็น `--text-sm`, `--text-base`, `--text-lg`

## Scales ในการทำงาน

ส่วนด้านล่างใช้เฉพาะ spacing และ type sizes ที่ใช้ token เท่านั้น เปลี่ยน `--text-base` หรือ `--space-4` ใน CSS และดู scale ที่ส่งผลตลอด

export const scalesHtml = `<article class="article">
  <p class="label">Case Study</p>
  <h1 class="heading-hero">Designing for scale</h1>
  <p class="lead">A consistent type scale and spacing system are the foundation of every readable, well-structured interface.</p>
  <h2 class="heading-section">Why consistency matters</h2>
  <p class="body">When every spacing decision comes from the same set of tokens, layouts feel harmonious — not because of any single value, but because all the values belong to the same system.</p>
  <p class="caption">Last updated: June 2026</p>
</article>`;

export const scalesCss = `:root {
  --text-xs: 0.75rem;
  --text-sm: 0.875rem;
  --text-base: 1rem;
  --text-lg: 1.25rem;
  --text-xl: 1.5rem;
  --text-2xl: 2rem;
  --text-3xl: 2.5rem;
  --space-1: 0.25rem;
  --space-2: 0.5rem;
  --space-4: 1rem;
  --space-6: 1.5rem;
  --space-8: 2rem;
  --space-12: 3rem;
  --color-text: #111827;
  --color-text-muted: #6B7280;
  --color-accent: #7C3AED;
}
body { font-family: system-ui, sans-serif; background: #f9f9f9; padding: var(--space-8); }
.article { max-width: 600px; }
.label {
  font-size: var(--text-xs);
  font-weight: 700;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--color-accent);
  margin: 0 0 var(--space-2);
}
.heading-hero {
  font-size: var(--text-2xl);
  font-weight: 800;
  color: var(--color-text);
  margin: 0 0 var(--space-4);
  line-height: 1.2;
}
.lead {
  font-size: var(--text-lg);
  color: var(--color-text-muted);
  margin: 0 0 var(--space-8);
  line-height: 1.6;
}
.heading-section {
  font-size: var(--text-xl);
  font-weight: 700;
  color: var(--color-text);
  margin: 0 0 var(--space-4);
}
.body {
  font-size: var(--text-base);
  color: var(--color-text);
  line-height: 1.7;
  margin: 0 0 var(--space-6);
}
.caption {
  font-size: var(--text-xs);
  color: var(--color-text-muted);
  margin: 0;
}`;

<LivePreview client:visible html={scalesHtml} css={scalesCss} />

<Tip title="ค่านอก scale เป็นสัญญาณ">
ถ้าค่า spacing ไม่อยู่ใน scale ให้ถามว่าทำไม มักหมายความว่าดีไซน์ต้องการการปรับเล็กน้อย — ช่องว่าง 13px น่าจะหมายถึง 12px หรือ 16px การแจ้งสิ่งเหล่านี้ในระหว่าง design review ถูกกว่ามากการแก้ไขหลังจากนักพัฒนาใช้งานแล้ว
</Tip>

export const quizScales = [
  {
    q: 'ทำไมดีไซเนอร์จึงมักใช้ 8px เป็น base spacing unit?',
    options: [
      'มันเป็นค่าเริ่มต้นของเบราว์เซอร์',
      'หน้าจอส่วนใหญ่หารด้วย 8px ลงตัว',
      'มันดูทันสมัย',
      'CSS กำหนดให้ใช้',
    ],
    answer: 1,
  },
  {
    q: 'Type scale กำหนดสิ่งใด?',
    options: [
      'เฉพาะสีของหัวข้อ',
      'ชุดขนาดฟอนต์ที่สอดคล้องกันด้วย ratio คงที่',
      'จำนวนฟอนต์บนเพจ',
      'เฉพาะ line heights',
    ],
    answer: 1,
  },
  {
    q: 'CSS property ใดที่คุณจะใช้ spacing token สำหรับ?',
    options: ['color', 'font-family', 'padding', 'opacity'],
    answer: 2,
  },
];

<Quiz client:visible id="workflow/scales" questions={quizScales} />

<ProgressTracker client:visible id="workflow/scales" />
