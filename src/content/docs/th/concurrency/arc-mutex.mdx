---
title: 'Arc และ Mutex'
description: 'การแชร์ mutable state ข้าม threads อย่างปลอดภัยด้วย Arc<Mutex<T>> ใน Rust — เทียบกับ JavaScript ที่ไม่มี shared mutable state ระหว่าง Workers'
---

import TsGo from '../../../../components/TsGo.tsx';
import Playground from '../../../../components/Playground.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import RsOnly from '../../../../components/RsOnly.astro';

## Shared mutable state: TypeScript vs Rust

ใน JavaScript Workers ไม่แชร์ memory โดยปริยาย แต่ละ Worker มี heap ของตัวเอง วิธีเดียวที่จะแชร์ mutable state คือผ่าน `SharedArrayBuffer` ร่วมกับ `Atomics` ซึ่งเป็น low-level มีโอกาสเกิด error ง่าย และแทบไม่ถูกใช้ใน application code

Rust รองรับ shared mutable state ข้าม threads ผ่านสอง type ที่ใช้ร่วมกัน:

- **`Arc<T>`** — Atomically Reference Counted เป็น thread-safe smart pointer ที่ให้หลาย thread own ค่าเดียวกันบน heap เมื่อ `Arc` clone สุดท้ายถูก drop ค่าจะถูก free
- **`Mutex<T>`** — Mutual exclusion lock ครอบค่า `T` และรับประกันว่ามีแค่ thread เดียวที่เข้าถึง inner value ได้ในแต่ละขณะ

รวมกัน `Arc<Mutex<T>>` คือ idiom มาตรฐานของ Rust สำหรับ shared mutable state ข้าม threads

## Arc — thread-safe reference counting

`Arc` คือคำตอบของ Rust สำหรับ "ฉันต้องการ owner หลายรายข้าม threads" มันคล้ายกับ `Rc<T>` (reference counter แบบ single-thread) แต่ใช้ **atomic operations** สำหรับ reference count — ปลอดภัยที่จะอัปเดตจากหลาย thread พร้อมกัน

คุณ clone `Arc` เพื่อให้ thread อื่นมี handle ไปยัง allocation เดียวกัน การ clone มีต้นทุนต่ำมาก: แค่ increment atomic integer

## Mutex — interior mutability พร้อม lock

`Mutex<T>` ครอบค่าและป้องกันด้วย lock ในการเข้าถึง `T` ภายใน คุณเรียก `.lock()` ซึ่ง:

1. Block จนกว่าไม่มี thread อื่นถือ lock
2. คืนค่า `MutexGuard<T>` — smart pointer ที่ dereference เป็น `&mut T`
3. Release lock อัตโนมัติเมื่อ `MutexGuard` ถูก drop (ปลาย scope)

นี่คือ **RAII locking** — pattern เดียวกับที่ Rust ใช้กับ memory: resource จะถูก release อัตโนมัติเมื่อ guard ออกจาก scope

export const arcMutexTs = `// TypeScript: ไม่มี shared mutable state ระหว่าง Workers โดยปริยาย
// SharedArrayBuffer + Atomics คือ primitive ระดับต่ำ
const sab = new SharedArrayBuffer(4);
const arr = new Int32Array(sab);

// Worker 1
Atomics.add(arr, 0, 1); // atomic increment

// Worker 2
Atomics.add(arr, 0, 1); // atomic increment

// Main thread
console.log(Atomics.load(arr, 0)); // 2

// ในทางปฏิบัติ TS apps ส่วนใหญ่หลีกเลี่ยง shared memory ระหว่าง Workers
// และใช้ message passing (postMessage) แทน`;

export const arcMutexRs = `use std::sync::{Arc, Mutex};
use std::thread;

fn main() {
    // Arc<Mutex<u32>>: shared, mutable counter ข้าม threads
    let counter = Arc::new(Mutex::new(0u32));
    let mut handles = vec![];

    for _ in 0..5 {
        let c = Arc::clone(&counter); // ถูก: แค่ increment atomic refcount
        let h = thread::spawn(move || {
            let mut val = c.lock().unwrap(); // acquire lock
            *val += 1;                       // mutate inner value
            // MutexGuard 'val' ถูก drop ที่นี่ — lock ถูก release
        });
        handles.push(h);
    }

    for h in handles { h.join().unwrap(); }

    // มีแค่ owner เดียวเหลืออยู่ — ปลอดภัยที่จะอ่านค่าสุดท้าย
    println!("final counter = {}", *counter.lock().unwrap()); // 5
}`;

<TsGo client:visible ts={arcMutexTs} go={arcMutexRs} />

## รวบรวมผลลัพธ์จากหลาย thread

Pattern ที่พบบ่อยคือให้แต่ละ thread push ผลลัพธ์เข้าไปใน `Vec` ที่แชร์กัน แล้วอ่านผลรวมหลังจาก thread ทั้งหมด join แล้ว

export const arcMutexCollectTs = `// TypeScript: รวบรวมผลลัพธ์ด้วย Promise.all
async function processAll(items: number[]): Promise<number[]> {
  return Promise.all(items.map(item =>
    Promise.resolve(item * item) // simulate async work
  ));
}`;

export const arcMutexCollectRs = `use std::sync::{Arc, Mutex};
use std::thread;

fn main() {
    let results: Arc<Mutex<Vec<u32>>> = Arc::new(Mutex::new(vec![]));
    let items = vec![1u32, 2, 3, 4, 5];
    let mut handles = vec![];

    for item in items {
        let r = Arc::clone(&results);
        let h = thread::spawn(move || {
            r.lock().unwrap().push(item * item);
        });
        handles.push(h);
    }

    for h in handles { h.join().unwrap(); }

    let mut res = results.lock().unwrap().clone();
    res.sort(); // sort เพื่อ output ที่แน่นอน
    println!("squares: {:?}", res); // squares: [1, 4, 9, 16, 25]
}`;

<TsGo client:visible ts={arcMutexCollectTs} go={arcMutexCollectRs} />

## ลองรันเลย

export const arcMutexCode = `use std::sync::{Arc, Mutex};
use std::thread;

fn main() {
    let counter = Arc::new(Mutex::new(0u32));
    let mut handles = vec![];

    for _ in 0..5 {
        let c = Arc::clone(&counter);
        let h = thread::spawn(move || {
            let mut val = c.lock().unwrap();
            *val += 1;
        });
        handles.push(h);
    }

    for h in handles {
        h.join().unwrap();
    }

    println!("final counter = {}", *counter.lock().unwrap());
}`;

<Playground client:visible code={arcMutexCode} />

<RsOnly title="RAII locking และ poison">

### RAII locking — unlock อัตโนมัติ

`MutexGuard<T>` implements `Drop` เมื่อ guard ออกจาก scope — ไม่ว่าจะออกแบบปกติหรือผ่าน panic — lock จะถูก release คุณไม่สามารถลืม unlock Mutex ใน Rust ได้โดยบังเอิญ

นี่เหมือนกับ RAII memory pattern: เช่นเดียวกับที่ `Box<T>` free memory เมื่อออกจาก scope `MutexGuard<T>` จะ release lock

### Poisoned Mutex

`lock()` คืนค่า `Result<MutexGuard<T>, PoisonError>` Mutex จะกลาย **poisoned** ถ้า thread panic ขณะถือ lock เหตุผลคือข้อมูลที่ถูกป้องกันอาจอยู่ในสถานะไม่สอดคล้องกัน การเรียก `.unwrap()` จะ panic ใน thread ปัจจุบันด้วย ใน production code คุณสามารถจัดการ `PoisonError` โดยเรียก `.into_inner()` เพื่อกู้คืน guard ถ้าคุณรู้ว่าข้อมูลนั้น consistent จริงๆ

### Arc vs Rc — อย่าใช้ Rc ข้าม threads

`Rc<T>` ใช้ plain (non-atomic) integer operations สำหรับ reference counting — ถูกกว่า `Arc` แต่ **ไม่ใช่ `Send`** Compiler จะปฏิเสธการย้าย `Rc` เข้าไปใน thread ใช้ `Rc` ภายใน single thread (หรือ `!Send` async context) และ `Arc` เมื่อต้องการข้ามขอบเขต thread

</RsOnly>

export const quizArcMutex = [
  {
    q: "Arc ใน Rust ย่อมาจากอะไร?",
    options: [
      "Async Reference Counter",
      "Atomically Reference Counted",
      "Allocated Reference Cell",
      "Automatic Resource Control"
    ],
    answer: 1
  },
  {
    q: "คุณ release Mutex lock ใน Rust ได้อย่างไร?",
    options: [
      "เรียก mutex.unlock()",
      "เรียก drop(guard) อย่างชัดเจน",
      "MutexGuard ถูก drop อัตโนมัติเมื่อออกจาก scope (RAII)",
      "เรียก mutex.release() ตอนปลาย thread"
    ],
    answer: 2
  },
  {
    q: "Mutex จะกลาย 'poisoned' เมื่อไหร่?",
    options: [
      "เมื่อถือ lock นานกว่า 100 milliseconds",
      "เมื่อสอง thread พยายาม lock พร้อมกัน",
      "เมื่อ thread panic ขณะถือ lock",
      "เมื่อ Arc reference count ถึงศูนย์"
    ],
    answer: 2
  },
  {
    q: "ทำไมต้องใช้ Arc<Mutex<T>> แทนที่จะใช้แค่ Mutex<T>?",
    options: [
      "Mutex<T> ไม่ implement Send",
      "Arc ให้หลาย thread own reference ไปยัง Mutex เดียวกันได้",
      "Mutex<T> ไม่สามารถใช้กับ threads ได้เลย",
      "Arc ให้ performance ที่ดีกว่า Mutex เพียงอย่างเดียว"
    ],
    answer: 1
  }
];

<Quiz client:visible id="concurrency/arc-mutex" questions={quizArcMutex} />

<ProgressTracker client:visible id="concurrency/arc-mutex" />
