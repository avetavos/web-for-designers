---
title: 'Database ด้วย SQLx'
description: 'sqlx queries, connection pool, async — เทียบกับ ORM ใน Node'
---

import TsGo from '../../../../components/TsGo.tsx';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';
import RsOnly from '../../../../components/RsOnly.astro';

## Prisma/TypeORM vs SQLx

ใน Node.js คุณอาจใช้ Prisma หรือ TypeORM — ORM ที่ generate queries จาก schema หรือ model decorators SQLx ใช้แนวทางที่แตกต่าง: คุณเขียน **raw SQL** แต่ compiler ตรวจสอบ queries กับ database จริง ที่ **compile time** โดยใช้ macro family `sqlx::query!` ไม่มี ORM ไม่มี query builder — แค่ SQL ที่ type-checked

| ฟีเจอร์ | Prisma/TypeORM | SQLx |
|---|---|---|
| รูปแบบ Query | ORM API / query builder | Raw SQL |
| Type safety | Generated types | Compile-time macro verification |
| Migrations | Built-in CLI | `sqlx migrate` CLI |
| Connection pool | จัดการภายใน | `PgPool` (explicit) |
| Async runtime | Node event loop | Tokio |

## ตั้งค่า pool

```toml
# Cargo.toml
[dependencies]
sqlx = { version = "0.7", features = ["postgres", "runtime-tokio-rustls", "uuid", "chrono"] }
```

export const tsPool = `// Node — Prisma client setup
import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();
export default prisma;`;

export const rsPool = `// Rust — SQLx PgPool setup
use sqlx::PgPool;
use std::env;

pub async fn create_pool() -> PgPool {
    let url = env::var("DATABASE_URL")
        .expect("DATABASE_URL must be set");
    PgPool::connect(&url)
        .await
        .expect("Failed to connect to database")
}`;

<TsGo client:visible ts={tsPool} go={rsPool} />

## Queries ด้วย `query_as!`

`sqlx::query_as!` map result row ของ SQL ไปยัง Rust struct โดยตรง ชื่อ column ต้องตรงกับชื่อ field ถ้า schema เปลี่ยนและ column หายไป code จะ **ไม่สามารถ compile ได้**

export const tsPrismaQuery = `// Prisma
const users = await prisma.user.findMany({
  where: { active: true },
  orderBy: { createdAt: 'desc' },
});

const user = await prisma.user.findUnique({
  where: { id },
});`;

export const rsSqlxQuery = `use sqlx::PgPool;
use uuid::Uuid;

#[derive(sqlx::FromRow, serde::Serialize)]
struct User {
    id: Uuid,
    name: String,
    email: String,
    active: bool,
}

// List query
async fn list_users(pool: &PgPool) -> sqlx::Result<Vec<User>> {
    sqlx::query_as!(
        User,
        "SELECT id, name, email, active FROM users WHERE active = true ORDER BY id"
    )
    .fetch_all(pool)
    .await
}

// Get one
async fn find_user(pool: &PgPool, id: Uuid) -> sqlx::Result<Option<User>> {
    sqlx::query_as!(
        User,
        "SELECT id, name, email, active FROM users WHERE id = $1",
        id
    )
    .fetch_optional(pool)
    .await
}`;

<TsGo client:visible ts={tsPrismaQuery} go={rsSqlxQuery} />

## Insert และ delete

```rust
use sqlx::PgPool;
use uuid::Uuid;

async fn create_user(
    pool: &PgPool,
    name: &str,
    email: &str,
) -> sqlx::Result<User> {
    sqlx::query_as!(
        User,
        "INSERT INTO users (id, name, email, active)
         VALUES ($1, $2, $3, true)
         RETURNING id, name, email, active",
        Uuid::new_v4(),
        name,
        email
    )
    .fetch_one(pool)
    .await
}

async fn delete_user(pool: &PgPool, id: Uuid) -> sqlx::Result<bool> {
    let result = sqlx::query!(
        "DELETE FROM users WHERE id = $1",
        id
    )
    .execute(pool)
    .await?;

    Ok(result.rows_affected() > 0)
}
```

## เชื่อม pool เข้ากับ Axum State

```rust
use axum::{extract::State, routing::get, Json, Router};
use sqlx::PgPool;
use std::sync::Arc;

#[derive(Clone)]
struct AppState { db: PgPool }

async fn list_users_handler(
    State(state): State<Arc<AppState>>,
) -> Result<Json<Vec<User>>, AppError> {
    let users = list_users(&state.db).await.map_err(AppError::Database)?;
    Ok(Json(users))
}

#[tokio::main]
async fn main() {
    let pool = create_pool().await;
    let state = Arc::new(AppState { db: pool });

    let app = Router::new()
        .route("/users", get(list_users_handler))
        .with_state(state);

    let listener = tokio::net::TcpListener::bind("0.0.0.0:3000").await.unwrap();
    axum::serve(listener, app).await.unwrap();
}
```

<RsOnly title="Compile-time query verification">

`sqlx::query_as!` และ `sqlx::query!` ใช้ procedural macro system ของ Rust เชื่อมต่อกับ database ของคุณ **ที่ compile time** (โดยใช้ environment variable `DATABASE_URL`) และตรวจสอบ:

- SQL ถูกต้องตาม syntax
- ทุก column ที่อ้างอิงมีอยู่จริงใน table
- Fields ของ Rust struct ตรงกับ column types ที่ return กลับมา

ถ้าคุณเปลี่ยนชื่อ column ใน migration และลืมอัปเดต query build จะล้มเหลวพร้อม error ที่ชัดเจน ซึ่งกำจัด bug ทั้ง class ที่มักจะเกิดขึ้นที่ runtime เมื่อใช้ Prisma หรือ TypeORM

Offline mode (`sqlx prepare`) บันทึก query metadata ไปยัง `sqlx-data.json` เพื่อให้ CI compile ได้โดยไม่ต้องมี database จริง

</RsOnly>

> **รันบนเครื่องของคุณ** — SQLx ต้องการ PostgreSQL database จริงและ environment variable `DATABASE_URL` รัน `sqlx migrate run` เพื่อ apply migrations จากนั้น `cargo run`

export const quizDatabase = [
  {
    q: "SQLx ตรวจสอบว่า SQL queries ถูกต้องเมื่อใด?",
    options: [
      "ที่ runtime เมื่อ query execute",
      "ที่ compile time โดยใช้ DATABASE_URL environment variable",
      "เฉพาะระหว่าง `sqlx test`",
      "ไม่เคย — SQLx เชื่อถือ raw SQL strings"
    ],
    answer: 1
  },
  {
    q: "SQLx method ใดดึง rows ทั้งหมดเป็น `Vec<T>`?",
    options: [
      "`.fetch_one(pool).await`",
      "`.execute(pool).await`",
      "`.fetch_all(pool).await`",
      "`.fetch_optional(pool).await`"
    ],
    answer: 2
  },
  {
    q: "`RETURNING id, name, email` ใน `INSERT` statement เปิดใช้อะไรใน SQLx?",
    options: [
      "มัน print inserted row ไปยัง stdout",
      "มันอนุญาตให้ `fetch_one` return inserted struct แทน row count",
      "มันเปิดใช้ compile-time verification เท่านั้น",
      "มันจำเป็นสำหรับ INSERT statements ทั้งหมดใน SQLx"
    ],
    answer: 1
  }
];

<Quiz client:visible id="api-axum/database-sqlx" questions={quizDatabase} />

<ProgressTracker client:visible id="api-axum/database-sqlx" />
